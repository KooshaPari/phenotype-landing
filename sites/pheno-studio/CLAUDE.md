# pheno-studio

Vue 3 + Vite single-page app — card grid that links to deployed phenotype
variants. Lives in the `phenotype-landing` monorepo as a sub-package.

## Commands

```bash
npm install
npm run dev
npm run build
```
